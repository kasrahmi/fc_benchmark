package utils

const (
    UFFDSocket    = "/tmp/firecracker-uffd.socket"
    APISocket     = "/tmp/firecracker.socket"
    RepoPath      = "/users/nehalem/fc_benchmark"
    FCVmmPath     = RepoPath + "/bin/firecracker"
    FCUffdPath    = RepoPath + "/bin/uffd_valid_count_periodic_handler"
    UbuntuPath    = RepoPath + "/ubuntu"
    ScriptsPath   = RepoPath + "/scripts"
    SnapshotPath  = RepoPath + "/snapshot"
    WorkloadPath  = RepoPath + "/workload"
    Log_Path       = RepoPath + "/logs"
)
